# Copyright 2021 Canonical Ltd.
# See LICENSE file for licensing details.

# NB none of these actions are implemented yet.

restart-pooler:
  description: Restarts pgbouncer application. This does not restart the container!

get-original-admin-password:
  description: Retrieves the pgbouncer admin password, generated on startup. If not already completed, please update this password using the update-admin-password Juju Action.

update-admin-password:
  description: Updates the admin password.

add-admin-user:
  description: Adds a new pgbouncer user.

get-userlist:
  description: gets all the pgbouncer users